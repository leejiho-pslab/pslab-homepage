# 네이버·구글 검색 미노출 — 원인 점검 결과 + 등록 가이드

## 점검 결과 요약

코드 쪽은 문제없습니다. `robots.txt`(전체 허용) · `sitemap.xml`(전 페이지 등록) · 메타 태그
(title/description/keywords) · Open Graph · 캐노니컬 URL · 구조화 데이터(JSON-LD) 모두
정상 설정돼 있고, `keywords`에 `pslab`·`문제해결연구소`·`ALWAYS ON`·`얼웨이즈온`이 이미
들어가 있습니다.

**진짜 원인 둘:**
1. **사이트가 아직 5일밖에 안 됐습니다** (2026-07-24 최초 배포). 검색엔진이 새 도메인을
   발견해 색인하는 데는 원래 며칠~몇 주가 걸립니다 — 등록 없이 그냥 기다려도 언젠가는
   뜨지만, 아래 등록을 하면 훨씬 빨라집니다.
2. **구글 서치콘솔에 이 사이트가 등록된 적이 없습니다.** 검증 태그/파일이 코드 어디에도
   없어서, 구글에게 "이 사이트 색인해줘"라고 알려준 적이 아예 없는 상태입니다.
   (네이버는 소유확인 메타 태그가 이미 코드에 심어져 있어 절반은 준비돼 있습니다 —
   서치어드바이저에서 등록·확인만 하면 됩니다.)

---

## 1. 네이버 서치어드바이저 (절반 완료 — 등록만 하면 됨)

1. https://searchadvisor.naver.com 접속 → 로그인
2. **웹마스터도구 → 사이트 등록** → `https://pslab.ai.kr` 입력
3. **소유확인** — 이미 사이트에 인증 태그가 심어져 있어(`naver-site-verification`),
   "HTML 태그" 방식을 선택하면 바로 확인될 겁니다. 확인 버튼만 누르시면 됩니다.
4. 등록 후 좌측 메뉴 **요청 → 사이트맵 제출** → `sitemap.xml` 입력 후 제출
   (`https://pslab.ai.kr/sitemap.xml`)
5. **요청 → 웹페이지 수집요청** → 홈(`https://pslab.ai.kr`)과 `/always-on` 페이지를
   각각 넣고 수집요청 — 이게 "빨리 좀 봐줘"라고 직접 요청하는 기능이라 가장 효과가 큽니다.

## 2. 구글 서치콘솔 (신규 등록 필요)

**추천 방법 — 도메인 속성 (코드 수정 없이 한 번에 끝남):**
1. https://search.google.com/search-console 접속 → 로그인
2. **속성 추가** → **도메인** 선택 → `pslab.ai.kr` 입력 (앞에 https:// 붙이지 않음)
3. 화면에 나오는 **TXT 레코드**를 복사
4. 도메인을 구매/관리하는 곳(가비아·후이즈 등 도메인 등록기관)의 **DNS 관리** 메뉴에서
   TXT 레코드 추가 → 방금 복사한 값 붙여넣기 → 저장
   (도메인을 어디서 사셨는지 모르시면 말씀해주세요 — 화면 캡처로 같이 찾아드릴게요)
5. DNS 반영은 몇 분~몇 시간 걸릴 수 있습니다. 반영되면 서치콘솔에서 **확인** 클릭

**대안 — URL 접두어 속성 (DNS 접근이 어려우실 때):**
1. **속성 추가** → **URL 접두어** → `https://pslab.ai.kr` 입력
2. **HTML 태그** 방식 선택 → `<meta name="google-site-verification" content="...">` 코드가
   나오면 그 **content=" " 안의 값만** 저를 통해 전달해주세요 — 제가 바로 코드에
   추가해서 배포해드리겠습니다 (네이버 태그와 같은 자리에 한 줄 추가하는 작업이라 5분이면 됩니다).

**등록 후 (둘 중 어느 방법이든):**
3. 좌측 메뉴 **Sitemaps** → `sitemap.xml` 제출
4. 상단 **URL 검사** 창에 `https://pslab.ai.kr` 입력 → **색인 생성 요청** 클릭
   (`/always-on` 페이지도 같은 방법으로 한 번 더)

---

## 예상 소요 시간

| | 등록만 했을 때 | 수집요청/색인생성요청까지 했을 때 |
|---|---|---|
| 네이버 | 며칠 내 | 수 시간~1~2일 |
| 구글 | 1~4주 | 며칠~1주 |

새 도메인이라 즉시 뜨지는 않지만, 위 등록·요청을 해두면 검색엔진이 "이 사이트가
존재한다"는 걸 바로 알게 돼 훨씬 빨리 잡힙니다.

---

# 등록 이후 — 색인 요청 (2026-07-29 추가)

서치콘솔 **등록(소유확인)과 색인은 별개**입니다. 등록만으로는 검색에 뜨지 않고,
색인 요청을 해야 구글이 실제로 페이지를 읽어 검색 결과에 넣습니다.

## 1단계 — 현재 색인 상태 확인
서치콘솔 상단 **URL 검사** 창에 `https://pslab.ai.kr` 입력 → 엔터

| 표시 | 의미 | 할 일 |
|---|---|---|
| URL이 Google에 등록되어 있지 않음 | 아직 색인 전 | 2단계 |
| 검색된 페이지 아님 | 발견은 됐으나 크롤링 전 | 2단계 |
| URL이 Google에 등록되어 있음 | 색인 완료 | 순위가 잡힐 때까지 대기 |

## 2단계 — 색인 생성 요청 (가장 효과 큼)
URL 검사 결과 화면 → **색인 생성 요청** 클릭 → 1~2분 테스트 후 대기열 등록.

**하루 한도가 10건 내외**이므로 우선순위대로:
1. `https://pslab.ai.kr` (홈)
2. `https://pslab.ai.kr/always-on` — 얼웨이즈온/ALWAYS ON 검색 대응
3. `https://pslab.ai.kr/about`
4. `https://pslab.ai.kr/magazine`
5. `https://pslab.ai.kr/portfolio`

## 3단계 — 사이트맵 제출
좌측 **Sitemaps** → `sitemap.xml` 입력 → 제출.

제출 직후 "가져올 수 없음"은 정상입니다. 몇 시간~하루 뒤 **상태: 성공**으로 바뀝니다.

> **검색된 페이지 수는 11개가 정상** (2026-07-29 기준).
> 매거진은 예약발행이라 `date`가 지난 글만 공개됩니다(`lib/magazine.ts`).
> 정적 6개(홈·about·portfolio·always-on·magazine·contact) + 공개된 글 5개 = 11.
> 남은 10개 글은 예정일(~8/21)이 되면 사이트맵에 자동 추가됩니다.
> 사이트맵은 1시간마다 재생성(`revalidate = 3600`)되므로 별도 재제출 불필요.

## 색인 여부 빠른 확인
구글에서 `site:pslab.ai.kr` 검색 → 결과가 하나라도 나오면 색인된 것.

## 예상 시간
색인 생성 요청 시 보통 **수 시간~3일**(요청 없으면 1~4주). 색인 ≠ 상위노출이며,
브랜드명("pslab" 등) 검색에서 자리를 잡는 데는 색인 후 며칠~2주가 더 걸립니다.
