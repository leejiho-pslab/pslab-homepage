# P.S.LAB 홈페이지 운영 점검 가이드

문제가 생겼을 때 **어디부터 보면 되는지** 정리한 문서입니다.

---

## 1. 사이트 상태 한 번에 확인하기

브라우저 주소창에 붙여넣으세요.

```
https://pslab.ai.kr/api/health
```

| 화면에 나오는 내용 | 뜻 | 할 일 |
|---|---|---|
| `{"ok":true,"db":"reachable", ...}` | 전부 정상 | 없음 |
| `{"ok":false,"db":"error", ...}` | DB가 멈춤 | 아래 2번 |
| `{"ok":true,"db":"not_configured"}` | Vercel 환경변수 누락 | 아래 3번 |
| 화면이 안 열림 | 사이트 자체 문제 | Vercel 배포 확인 |

---

## 2. 어드민 로그인이 안 되거나, 문의가 대시보드에 안 쌓일 때

### 증상

- 텔레그램 알림은 오는데 `https://pslab.ai.kr/admin` 목록에 안 나타남
- 어드민 로그인이 실패함

### 원인

**Supabase 무료 플랜은 일정 기간(약 7일) 요청이 없으면 프로젝트를 자동 일시정지**합니다.
정지되면 DB 저장·어드민 로그인이 멈춥니다. 홈페이지와 문의 폼 자체는 계속 정상 작동하고,
텔레그램 알림도 계속 발송되므로 **문의를 놓치지는 않지만 대시보드에는 기록이 남지 않습니다.**

### 복구 방법

1. 아래 주소를 브라우저 주소창에 붙여넣기

   ```
   https://supabase.com/dashboard/project/rnjhcrpurfanbzxwtegv
   ```

2. `Continue with GitHub` 로 로그인
3. 화면에 `Project is paused` 가 보이는지 확인
4. `Restore project` (또는 `Resume project`) 버튼 클릭
5. 3~5분 기다리기
6. 1번의 상태확인 주소로 `"ok":true` 확인

> 복구해도 프로젝트 주소·키는 그대로이므로 **Vercel 설정은 건드릴 필요 없습니다.**

### 재발 방지 (이미 적용됨)

`.github/workflows/supabase-keepalive.yml` 이 **매주 월·목 09:10(KST)** 에 상태확인 주소를
자동 호출해 DB를 깨웁니다. 정지 기준보다 훨씬 짧은 간격이라 다시 정지되지 않습니다.
실패하면 GitHub Actions에 빨간 표시로 남으므로 모르고 지나가지 않습니다.

수동으로 지금 바로 돌려보려면: GitHub 저장소 → `Actions` 탭 → 왼쪽에서 해당 워크플로 선택 →
`Run workflow` 버튼.

---

## 3. 문의 알림이 아예 안 올 때

Vercel 환경변수 4개가 모두 등록되어 있는지 확인하세요.

```
https://vercel.com/dashboard
```

프로젝트 → `Settings` → `Environment Variables`

필요한 이름 4개:

```
NEXT_PUBLIC_SUPABASE_URL
```

```
NEXT_PUBLIC_SUPABASE_ANON_KEY
```

```
TELEGRAM_BOT_TOKEN
```

```
TELEGRAM_CHAT_ID
```

값을 바꾼 뒤에는 **재배포(Redeploy)** 해야 반영됩니다.

---

## 4. 문의 항목(7개)이 한 덩어리로 보일 때

대시보드 본문에 `[회사] … [담당자] …` 처럼 뭉쳐서 나오면, DB 테이블에 확장 칸이 없는 상태입니다.
아래 SQL을 Supabase SQL Editor에서 한 번 실행하면 해결됩니다. (기존 문의는 그대로 유지)

```
https://supabase.com/dashboard/project/rnjhcrpurfanbzxwtegv/sql/new
```

```sql
alter table public.inquiries
  add column if not exists company  text,
  add column if not exists manager  text,
  add column if not exists "position" text,
  add column if not exists phone    text,
  add column if not exists email    text,
  add column if not exists budget   text;
```

`Success. No rows returned` 이 나오면 완료입니다.

> 코드(`app/api/inquiry/route.ts`)는 확장 칸이 없어도 옛 형식으로 저장을 시도하므로
> **정보가 유실되지는 않습니다.** 보기 좋게 정리되는 문제일 뿐입니다.

---

## 5. 매거진 글이 안 올라올 때

- 글은 `content/magazine/` 에 날짜별로 미리 작성되어 있고, **날짜가 되면 자동 공개**됩니다.
- 예약분이 떨어지면 새 글을 채워야 합니다.
- 사이트맵·RSS는 `force-dynamic` 이라 새 글이 즉시 반영됩니다.

확인용 주소:

```
https://pslab.ai.kr/sitemap.xml
```

```
https://pslab.ai.kr/magazine/rss.xml
```

---

## 사고 기록

### 2026-08-04 · Supabase 자동 일시정지

- **증상**: 어드민 로그인 불가, 7/27 이후 문의가 대시보드에 없음
- **진단**: `rnjhcrpurfanbzxwtegv.supabase.co` 가 공개 DNS에서 NXDOMAIN(주소 없음) 응답.
  대조군(`supabase.com`, `pslab.ai.kr`)은 정상 → Supabase 프로젝트 정지로 확정
- **영향**: 정지 기간(7/27~8/4) 동안 들어온 문의는 **텔레그램에만 남고 DB에는 미저장**.
  사이트·문의 폼·텔레그램 알림은 정상 작동
- **조치**: 프로젝트 Restore + keepalive 워크플로 도입 + 문의 확장 칸 6개 추가
